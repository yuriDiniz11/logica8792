#include<stdio.h>
#include<windows.h>


int main(){

    SetConsoleCP(65001);
    SetConsoleOutputCP(65001);

   for(int i = 0; i < 10; i++){
        printf("%s\n %d", "\nOs números estão aumentando: ---->>" ,i);
}

    printf("\nMeu for funcionou!!");

    return 0;
}